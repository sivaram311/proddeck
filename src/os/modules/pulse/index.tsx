"use client";

import { ModuleStub } from "../_shared/ModuleStub";

export function PulseView() {
  return (
    <ModuleStub
      id="pulse"
      title="Pulse"
      blurb="Machine health: drives E–H, RAM/load, Postgres :5432, CSS :5900. Stack Pilot deep-link next."
    />
  );
}
