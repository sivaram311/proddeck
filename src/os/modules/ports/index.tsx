"use client";

import { ModuleStub } from "../_shared/ModuleStub";

export function PortsView() {
  return (
    <ModuleStub
      id="ports"
      title="Ports"
      blurb="MyAgent port registry vs live listeners. Reserve via job — no silent bind."
    />
  );
}
