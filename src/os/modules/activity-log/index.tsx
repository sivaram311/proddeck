"use client";

import { ModuleStub } from "../_shared/ModuleStub";

export function ActivityLogView() {
  return (
    <ModuleStub
      id="activity-log"
      title="Activity Log"
      blurb="Read-only ACTIVITY-LOG tail/filter by provider. Continue → Dispatch."
    />
  );
}
