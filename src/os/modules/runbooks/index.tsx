"use client";

import { ModuleStub } from "../_shared/ModuleStub";

export function RunbooksView() {
  return (
    <ModuleStub
      id="runbooks"
      title="Runbooks"
      blurb="Remember categories → runbook → Dispatch / Yard / Pulse links."
    />
  );
}
