"use client";

import { ModuleStub } from "../_shared/ModuleStub";

export function YardView() {
  return (
    <ModuleStub
      id="yard"
      title="Yard · Crew Fabric"
      blurb="Mission templates + parallel skill lane board (promote-q1, feature-ship, incident). See CLOUD-OS-ROADMAP §8."
    />
  );
}
