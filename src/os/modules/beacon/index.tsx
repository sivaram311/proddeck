"use client";

import { ModuleStub } from "../_shared/ModuleStub";

export function BeaconView() {
  return (
    <ModuleStub
      id="beacon"
      title="Beacon"
      blurb="Known hosts → upstream health (css, portal, agentverse, home, control, hdrive)."
    />
  );
}
